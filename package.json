{
  "name": "ws-domain",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "main": "domain.js",
  "scripts": {
    "prebuild": "tsc",
    "build": "vite build",
    "postbuild": "cd ./dist && npm link"
  },
  "devDependencies": {
    "standard-version": "^9.5.0",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
