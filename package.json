{
  "name": "ws-domain",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "main": "domain.js",
  "scripts": {
    "prebuild": "tsc",
    "build": "vite build",
    "prepublish": "cd ./public && npm run release && npm run build",
    "publish": "cd ./dist && npm link",
    "postpublish": "git push --follow-tags origin master"
  },
  "devDependencies": {
    "standard-version": "^9.5.0",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
